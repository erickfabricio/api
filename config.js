const config = {};

config.port = process.env.PORT || 3000;

config.db = 'mongodb://localhost/api-admin';

config.key = "erickfabricioKEY"

config.key.playload = "infoMartínez" //

module.exports = config;